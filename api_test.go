package main

import "testing"

func TestHTTPServer(t *testing.T) {
	// transport := transports.NewLogTransport()
	// q := NewQueue("testing", transport)
	//
	// ts := httptest.NewServer(createHTTPServer(q, transport))
}
